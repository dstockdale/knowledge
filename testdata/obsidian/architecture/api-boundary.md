# API Boundary

## Context

The API boundary document has no YAML frontmatter because it came from an Obsidian-first docs workflow.

## Decision

Keep public graph reads separate from authenticated viewer-aware reads.
